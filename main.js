const app = Vue.createApp({ // Options object. At least an empty object.
    data: function() {
        return {
            product: "Socks",
            description: "Socks are great for wearing!"
        }
    }
})